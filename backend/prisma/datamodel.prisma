type User {
  id: ID! @id
  username: String! @unique
  email: String! @unique
  password: String!
  posts: [Post!]!
}

type Post {
  id: ID! @id
  content: String!
  postedBy: User!
  createdAt: DateTime! @createdAt
}
