type User {
  id: ID! @id
  username: String! @unique
  email: String! @unique
  password: String!
  avatar: String
  posts: [Post!]!
  comments: [Comment!]!
  friends: [User!]!
}

type Post {
  id: ID! @id
  content: String!
  postedBy: User!
  createdAt: DateTime! @createdAt
  comments: [Comment!]!
}

type Comment {
  id: ID! @id
  content: String!
  postedOn: Post!
  postedBy: User!
  createdAt: DateTime! @createdAt
}
